apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paymentsapi-mongodb-pvc
  namespace: egs-uniride
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi